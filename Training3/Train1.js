//convert string to integer

function convertToInteger(str) {

    const newNumber= parseInt(str); // convert string to integer
    return newNumber;
    }
    
    convertToInteger("56");


    function convertToIntegerFromBase(str) {


        return parseInt(str, 2);// Specifies the base of 2 for the number in the bracket
        }
        
        convertTconvertToIntegerFromBaseoInteger("10011");