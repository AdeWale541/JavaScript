// The best code are those that don't require mutation of existing values
// Always pass dependencies into functions as argument so it is more obvious
// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line

return fixedValue + 1
  // Only change code above this line
}